package template

/*
from collections import deque

q = deque()
visited = [False] * n

q.append(s)
visited[s] = True

while q:
  tmp = q.popleft()
  for i in e[tmp]:
    if not visited[i]:
      q.append(i)
      visited[i] = True
*/
